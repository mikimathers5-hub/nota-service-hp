name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type'
        required: true
        default: 'debug'
        type: choice
        options:
          - debug
          - release

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      # ==========================================
      # 1. CHECKOUT REPOSITORY
      # ==========================================
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # ==========================================
      # 2. SETUP NODE.JS
      # ==========================================
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # ==========================================
      # 3. INSTALL DEPENDENCIES
      # ==========================================
      - name: Clean npm cache
        run: npm cache clean --force

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      # ==========================================
      # 4. SETUP JAVA (JDK 17)
      # ==========================================
      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # ==========================================
      # 5. SETUP ANDROID SDK
      # ==========================================
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK components
        run: |
          sdkmanager "platforms;android-34"
          sdkmanager "build-tools;34.0.0"
          sdkmanager "platform-tools"
          sdkmanager "cmdline-tools;latest"

      # ==========================================
      # 6. PREPARE WEB ASSETS (www)
      # ==========================================
      - name: Prepare www directory
        run: |
          mkdir -p www
          
          # Copy index.html
          if [ -f "index.html" ]; then
            cp index.html www/
            echo "✓ Copied index.html"
          elif [ -f "www/index.html" ]; then
            echo "✓ index.html already in www"
          else
            echo "❌ ERROR: index.html not found!"
            ls -la
            exit 1
          fi
          
          # Copy manifest.json (atau buat default)
          if [ -f "manifest.json" ]; then
            cp manifest.json www/
          else
            echo '{"name":"Nota Service HP","short_name":"NotaService","start_url":".","display":"standalone","background_color":"#667eea","theme_color":"#667eea"}' > www/manifest.json
          fi
          
          # Copy service-worker.js (opsional)
          cp service-worker.js www/ 2>/dev/null || echo '// Service Worker Placeholder' > www/service-worker.js
          
          # Create placeholder icons
          touch www/icon.png
          touch www/icon-512.png
          
          echo "=== www directory contents ==="
          ls -la www/

      # ==========================================
      # 7. ADD ANDROID PLATFORM
      # ==========================================
      - name: Clean old Android folder
        run: rm -rf android

      - name: Add Android Platform
        run: npx cap add android

      # ==========================================
      # 8. UPDATE ANDROID MANIFEST (FULL PERMISSIONS)
      # ==========================================
      - name: Update AndroidManifest.xml with permissions
        run: |
          cat > android/app/src/main/AndroidManifest.xml << 'MANIFEST_EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">

              <!-- CONTACTS PERMISSIONS -->
              <uses-permission android:name="android.permission.READ_CONTACTS" />
              <uses-permission android:name="android.permission.WRITE_CONTACTS" />
              <uses-permission android:name="android.permission.GET_ACCOUNTS" />
              
              <!-- CAMERA PERMISSIONS -->
              <uses-permission android:name="android.permission.CAMERA" />
              <uses-feature android:name="android.hardware.camera" android:required="false" />
              <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
              
              <!-- BLUETOOTH PERMISSIONS -->
              <uses-permission android:name="android.permission.BLUETOOTH" />
              <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
              <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
              <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
              <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
              <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
              
              <!-- STORAGE PERMISSIONS -->
              <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
              <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
              <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
              
              <!-- NETWORK PERMISSIONS -->
              <uses-permission android:name="android.permission.INTERNET" />
              <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

              <application
                  android:allowBackup="true"
                  android:icon="@mipmap/ic_launcher"
                  android:label="@string/app_name"
                  android:roundIcon="@mipmap/ic_launcher_round"
                  android:supportsRtl="true"
                  android:theme="@style/AppTheme"
                  android:requestLegacyExternalStorage="true">
                  
                  <activity
                      android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
                      android:name=".MainActivity"
                      android:label="@string/title_activity_main"
                      android:theme="@style/AppTheme.NoActionBarLaunch"
                      android:launchMode="singleTask"
                      android:exported="true"
                      android:screenOrientation="portrait">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>

                  <provider
                      android:name="androidx.core.content.FileProvider"
                      android:authorities="${applicationId}.fileprovider"
                      android:exported="false"
                      android:grantUriPermissions="true">
                      <meta-data 
                          android:name="android.support.FILE_PROVIDER_PATHS" 
                          android:resource="@xml/file_paths" />
                  </provider>
              </application>

          </manifest>
          MANIFEST_EOF
          
          echo "✓ AndroidManifest.xml updated with full permissions"

      # ==========================================
      # 9. COPY CAPACITOR CONFIG
      # ==========================================
      - name: Copy Capacitor Config
        run: |
          cp capacitor.config.json android/ 2>/dev/null || true
          echo "=== android directory ==="
          ls -la android/

      # ==========================================
      # 10. SYNC CAPACITOR
      # ==========================================
      - name: Sync Capacitor
        run: npx cap sync android

      # ==========================================
      # 11. BUILD APK
      # ==========================================
      - name: Build Debug APK
        if: github.event.inputs.build_type != 'release'
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --stacktrace --info

      - name: Build Release APK
        if: github.event.inputs.build_type == 'release'
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease --stacktrace --info

      # ==========================================
      # 12. VERIFY BUILD OUTPUT
      # ==========================================
      - name: Verify APK exists
        run: |
          BUILD_TYPE="${{ github.event.inputs.build_type || 'debug' }}"
          APK_PATH="android/app/build/outputs/apk/${BUILD_TYPE}"
          
          echo "Looking for APK in: ${APK_PATH}"
          ls -la ${APK_PATH}/ || echo "Directory not found"
          
          # Find any APK file
          find android/app/build/outputs -name "*.apk" -type f

      # ==========================================
      # 13. UPLOAD APK ARTIFACT
      # ==========================================
      - name: Upload Debug APK
        if: github.event.inputs.build_type != 'release'
        uses: actions/upload-artifact@v4
        with:
          name: NotaService-Debug-v${{ github.run_number }}
          path: android/app/build/outputs/apk/debug/*.apk
          retention-days: 30
          if-no-files-found: warn

      - name: Upload Release APK
        if: github.event.inputs.build_type == 'release'
        uses: actions/upload-artifact@v4
        with:
          name: NotaService-Release-v${{ github.run_number }}
          path: android/app/build/outputs/apk/release/*.apk
          retention-days: 30
          if-no-files-found: warn

      # ==========================================
      # 14. CREATE GITHUB RELEASE (ON TAG)
      # ==========================================
      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          files: android/app/build/outputs/apk/**/*.apk
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ==========================================
      # 15. BUILD SUMMARY
      # ==========================================
      - name: Build Summary
        run: |
          echo "=========================================="
          echo "  BUILD COMPLETED SUCCESSFULLY! 🎉"
          echo "=========================================="
          echo "  Build Type: ${{ github.event.inputs.build_type || 'debug' }}"
          echo "  Run Number: ${{ github.run_number }}"
          echo "  Commit: ${{ github.sha }}"
          echo "=========================================="
